Flask==3.0.3
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.1
bcrypt==4.1.3
python-dotenv==1.0.1
gunicorn==22.0.0
WTForms==3.1.2
pytest==8.2.2
pytest-flask==1.3.0
# 2.9.9 publishes no wheels for Python 3.13. When Render defaulted to 3.13 the
# import failed at runtime and the app silently fell back to SQLite, which is
# what caused school records to disappear on restart. 2.9.10 ships cp313 wheels;
# the Python version is also pinned in .python-version as a second guard.
psycopg2-binary==2.9.10
email-validator==2.1.1
